import CVForm from "@/components/pdf/CVForm";
import { NextPage } from "next";

const Page: NextPage = ({}) => {
  return (
    <div className="bg-[#1F1F22]">
      <CVForm />
    </div>
  );
};

export default Page;
